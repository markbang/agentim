- Extend `routing_rules` from exact/prefix/priority matching to even richer policy routing only if needed (workspace/team patterns or regex), but avoid turning startup config into a DSL too early.
- Add more platform-native verification adapters where practical (Telegram secret token, Discord interaction signatures, and Feishu payload-token verification now exist; next candidates are QQ native signature schemes, or a stronger Feishu signed adapter if worth the complexity).
- Build on the current bounded-history behavior with better summary/retention strategies (higher-quality semantic summaries or periodic checkpoint summaries); prompt-window sizing is now separately configurable and long summaries now truncate on fragment boundaries, but the retained-summary content is still just deterministic string compaction.
- Add production-oriented execution controls around real agent backends (circuit-breaker-ish backpressure, maybe bounded retry backoff/jitter, and richer operator-facing failure telemetry) now that timeout control, retries, and 502/504 classification exist for the real OpenAI-compatible HTTP agent path.
